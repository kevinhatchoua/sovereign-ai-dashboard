# MVP Baseline

The **current version** of the app (as of the last full cleanup) is the **MVP baseline**.

When asked to **revert to MVP**, **defer to the default MVP**, or **restore the MVP**, restore behavior and code to match this baseline and avoid breaking working features that were part of it.
